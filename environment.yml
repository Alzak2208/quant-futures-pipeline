name: proj_ml_fin
channels:
  - conda-forge
dependencies:
  - python>=3.10
  - pip
  # All Python dependencies are declared in pyproject.toml (single source of
  # truth) and installed via the editable install below, including dev extras.
  - pip:
      - -e .[dev]
